# HOW TO USE THIS FILE ?
# Rename this file to .env and fill in the required values below before running the dhan_risk_manager.py script.
# Remember to restart dhan_Risk_manager.py after making changes to this file.

# CAUTION: Never commit sensitive information like API keys or passwords directly in the code.
# Instead, use environment variables or a separate configuration file that is not tracked by version control.

# API keys
# Create a dhan_token.txt file and paste Dhan Access token in that file or and set the token below
# DhanHQ API Credentials
DHAN_ACCESS_TOKEN=your_dhan_access_token_here

# Risk Management Settings
# Stoploss threshold: negative to trigger on loss (e.g., -2000), 0 for breakeven
DAILY_STOPLOSS=-5000
# Target profit to stop trading (positive value)
DAILY_TARGET=10000
# How often to check PNL (in seconds)
CHECK_INTERVAL_SECONDS=1

# Market Hours (24h format HH:MM)
MARKET_START_TIME=09:15
MARKET_END_TIME=15:30

# Per-Position Risk Management
ENABLE_POSITION_PERCENT_TAKE=False
POSITION_PERCENT_TAKE=5.0
ENABLE_POSITION_PERCENT_STOPLOSS=False
POSITION_PERCENT_STOPLOSS=2.0

# Trailing Stoploss Settings
ENABLE_TRAILING_STOPLOSS=False
TRAILING_STOPLOSS_ACTIVATE_PROFIT=1000
TRAILING_STOPLOSS_TRAIL_PERCENT=10

# Kill Switch (Activates Dhan's Kill Switch API on limit breach)
ENABLE_KILL_SWITCH=False

# Days to run the script: WEEKDAYS, WEEKENDS, ALL, or comma-separated (e.g. MON,WED)
RUN_DAYS=WEEKDAYS

# Logging Configuration
# LOG_FILE=dhan_risk_manager.log
LOG_LEVEL=INFO

# Telegram Notification Settings
TELEGRAM_ENABLED=False
TELEGRAM_BOT_TOKEN=your_telegram_bot_token
TELEGRAM_CHAT_ID=your_telegram_chat_id

# Send PNL status on every check (can be spammy if interval is low)
SEND_PNL_UPDATES=False
# Send periodic PNL updates (in seconds), independent of check interval. Set to 0 to disable this when SEND_PNL_UPDATES is True.
TELEGRAM_PNL_INTERVAL_SECONDS=600 # 600 seconds = 10 minutes

# Only send alerts when limits are breached
SEND_ONLY_ALERTS=True
